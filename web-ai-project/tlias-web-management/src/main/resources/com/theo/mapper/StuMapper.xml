<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.theo.mapper.StuMapper">

    <!--分页查询学生信息-->
    <select id="list" resultType="com.theo.pojo.Student">
        select s.* ,c.name as clazz_name from student s join clazz c on s.clazz_id=c.id
        <where>
            <if test="name != null and name != ''">
                and s.name like concat('%', #{name}, '%')
            </if>
            <if test="degree != null">
                and s.degree = #{degree}
            </if>
            <if test="clazzId != null">
                and s.clazz_id = #{clazzId}
            </if>
        </where>
    </select>
    <select id="getStuById" resultType="com.theo.pojo.Student">
        select s.* ,c.name as clazz_name from student s join clazz c on s.clazz_id=c.id
        where s.id = #{id}
    </select>

    <!--    根据id批量删除学生信息-->
    <delete id="deleteByIds">
        delete from student where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

<!--    添加学生信息-->
    <insert id="add">
        insert into student (name,no,gender,phone,id_card,is_college,address,degree,graduation_date,clazz_id,create_time,update_time) values
        (#{name},#{no},#{gender},#{phone},#{idCard},#{isCollege},#{address},#{degree},#{graduationDate},#{clazzId},#{createTime},#{updateTime})
    </insert>
<!--    更新学生信息-->
    <update id="update">
        UPDATE student
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="no != null and no != ''">no = #{no},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="degree != null">degree = #{degree},</if>
            <if test="idCard != null and idCard != ''">id_card = #{idCard},</if>
            <if test="isCollege != null">is_college = #{isCollege},</if>
            <if test="address != null and address != ''">address = #{address},</if>
            <if test="graduationDate != null">graduation_date = #{graduationDate},</if>
            <if test="violationCount != null">violation_count = #{violationCount},</if>
            <if test="violationScore != null">violation_score = #{violationScore},</if>
            <if test="clazzId != null">clazz_id = #{clazzId},</if>
        </set>
        WHERE id = #{id}
    </update>
    <!--获取学生学历分布数据-->
    <select id="getStudentDegreeData" resultType="java.util.Map">
        select (case when degree=1 then '初中'
        when degree=2 then '高中'
        when degree=3 then '大专'
        when degree=4 then '本科'
        when degree=5 then '硕士'
        else '其他' end) name,
        count(*) value from student group by degree order by value;
    </select>
    <select id="getStudentCountData" resultType="java.util.Map">
        select c.name clazzList, count(*) dataList from student s join clazz c on s.clazz_id=c.id group by c.name;
    </select>
</mapper>